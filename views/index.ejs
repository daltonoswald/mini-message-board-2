<html>
    <head>
        <title> <%= title %> </title>
    </head>
    <body>
        <% for (let i = 0; i < messages.length; i++) { %>
                <p><%= messages[i].text %></p>
                <p>By <%= messages[i].username %></p>
                <p>Posted on <%= messages[i].added %></p>
                <a href="/messages/<%= messages[i].id %>" id="<%= messages[i].id %>">Open</a>
        <% } %>
        <%- include('form') %>
    </body>
</html>